# 小程序核算周期经营 V1 进度跟踪

## 基本信息

- 开发分支：`codex/miniprogram-period-v1`
- 父仓库：`/Users/sirius/Documents/TRAE/JunSong-Cloud`
- 小程序子仓库：`junsong-miniprogram`
- 范围：V1.0 经营核心与稳定性、V1.1 库存和表单能力、V1.2 权限与技术债
- 主分支合并：人工验收通过后再执行
- PROD 发布：合并后由用户确认，不在本阶段自动执行

## 状态

| 阶段 | 状态 | 说明 |
|---|---|---|
| 设计确认 | 已完成 | 已固定 V1.0/V1.1/V1.2 范围，后续按阶段门禁推进 |
| 分支隔离 | 已完成 | 父仓库和小程序子仓库均已创建 `codex/miniprogram-period-v1` |
| V1 基线测试 | 已完成 | 已建立静态基线并观察到会话入口和首页口径缺口 |
| 登录态恢复 | 已完成首版 | 已增加统一 restoreSession、刷新 Token 回写和并发恢复合并；待 DEV 真机验证 |
| 周期摘要接口 | 已复用 | 复用现有 `/finance/accountingPeriod/current` 实时刷新接口，暂不新增重复后端接口 |
| 周期经营首页 | 已完成首版 | 已将首屏 KPI 改为实际缴款、周期净利、距离回本、周期总成本，加入角色标签、无周期状态和缓存数据提示 |
| V1.0 首页节流 | 已完成首版 | 秒杀和费用摘要等低频请求 60 秒内不重复加载；手工刷新和部门切换强制刷新，周期 KPI 不受影响 |
| V1.0 统一响应处理 | 已完成首版 | 已增加归一化测试和 `listData/getData` 接入，小程序构建已通过 |
| V1.0 字典缓存 | 已完成首版 | 已实现 10 分钟缓存、过期刷新、失败使用旧值、并发请求合并，并接入库存调整/流水；小程序构建已通过 |
| V1.0 部门切换竞态 | 已完成首版 | 请求层已有 contextVersion 丢弃旧响应；库存调整写操作增加 `contextSensitive` 二次校验，部门切换后不误处理旧响应 |
| V1.1 库存流水分页筛选 | 已完成首版 | 已支持页码、加载更多、日期范围、变动类型和部门切换重置；专项测试 2 项通过，微信构建通过 |
| V1.1 多商品库存调整与全流程幂等 | 已完成首版 | 调整单支持多商品行，逐行校验数量/成本；更新、删除、校验、提交、审批、过账使用确定性动作幂等键；专项测试 3 项通过，微信构建通过 |
| V1.1 草稿、表单组件、统一状态组件 | 已完成开发 | 新建表单草稿自动保存/恢复/提交清理已完成；采购商品明细、通用字段、费用 OCR 已组件化；`StateView` 已接入库存流水、库存调整和通用表单页；费用和进货业务校验已纳入专项回归 |
| V1.2 页面权限守卫与能力按钮 | 已完成开发 | 已统一拦截动态模块路由；库存调整按钮按后端精确权限能力显示，服务端 403 仍为最终边界 |
| V1.2 秒杀批量接口 | 已完成开发 | 新增按活动批量统计接口，首页由 N+1 请求改为一次批量请求，并保留部门数据范围 |
| V1.2 Vite patch 与错误上报 | 已完成开发 | 已补齐运行时 patch 原因、锁定版本和验证文档；小程序错误脱敏、排队、上报及服务端日志接收已完成 |
| DEV FILE 模块 | 已修复并验证 | FILE 排除无数据库场景下的 MyBatis/幂等自动配置；同时修复退出容器部署时未注入新 JAR 的脚本缺陷；容器已注册 Nacos，`9300/actuator/health` 返回 200 |
| DEV 部署 | 未开始 | 代码和专项测试通过后执行 |
| 人工验收 | 未开始 | 验收通过后才允许合并主分支 |
| 主分支合并 | 未开始 | 用户确认后执行 |

## 已知约束

1. 不覆盖 `main` 上已有财务和库存提交。
2. V1.0、V1.1、V1.2 均在本分支，但严格按阶段提交和门禁推进；字典缓存属于 V1.0，不再归入 V1.1。
3. 后端权限、租户和部门范围是最终边界，前端隐藏不代替服务端校验。
4. 周期净利必须使用实际缴款，不使用销售额作为收入。
5. 小程序子仓库必须先独立提交，再更新父仓库 gitlink。
6. FILE 服务启动修复单独记录，不能用“容器重启成功”替代应用真正启动成功；必须看到 Spring Boot started、注册成功和健康检查通过。

## 变更记录

### 2026-08-03

- 完成 V1 设计确认。
- 创建父仓库和小程序子仓库隔离分支。
- 生成设计说明、开发计划和进度跟踪文档。
- 建立 `scripts/miniprogram-period-v1.test.mjs`，基线测试已由失败转为通过。
- 小程序会话测试 `test/auth-session.test.mjs` 已通过。
- 小程序微信构建已通过；当前代码尚未部署 DEV。
- 周期首页静态基线测试已通过。
- 已完成首版角色标签：店面、授权门店、投资范围。
- 已完成无当前周期和最近成功数据提示。
- 已修正版本边界：字典缓存和统一响应处理归入 V1.0；库存/表单归入 V1.1；权限/技术债归入 V1.2。
- 已发现并修复 DEV FILE 服务启动失败：公共幂等 Mapper 被无数据库服务加载，FILE 已排除 MyBatis/幂等自动配置并验证健康。
- 已完成 V1.0 字典缓存和统一响应处理首版及专项测试；小程序微信构建通过。
- FILE 服务已部署 DEV 验证通过：容器运行、Nacos 注册、`9300/actuator/health` 返回 200。
- 已修复退出容器部署时未注入新 JAR 的脚本缺陷，避免部署报告成功但实际运行旧包。

### 2026-08-04

- 对齐库存调整回归测试与 V1.1 多商品模型，移除对旧单商品字段结构的错误断言。
- 完成通用表单字段组件 `FormField`：必填和选填字段统一渲染，继续复用父页面的日期、下拉、区域、图片、数字过滤和业务联动处理。
- 编辑态表单详情加载纳入统一 loading/error 状态，避免页面提前结束加载态。
- V1.1 专项测试、库存/财务基线测试和小程序微信构建通过；全量测试仍有与本轮无关的既有盘点、会员模糊搜索和秒杀搜索断言失败，暂不纳入本轮。
- 费用表单 OCR 区域已拆为 `ExpenseForm`，与采购明细、通用字段组件形成分层表单结构。
- 草稿隔离已补齐用户维度：通用表单和现场作业按模块、部门、用户隔离；旧现场作业调用仍兼容未传用户ID的旧键格式。
- V1.1 开发门禁已完成：库存、幂等、草稿、表单组件、费用/进货校验、统一状态和专项回归均通过；剩余仅为 DEV 部署和人工验收。

### 2026-08-04 V1.2

- 页面权限统一守卫已接入 `navigateTo`、`redirectTo`、`reLaunch` 和 `switchTab`，动态模块页按模块权限拦截，未授权时在进入页面前提示。
- 库存调整新增后端权限能力映射：新增/编辑、删除、审批、过账分别由 `finance:stockInit:add/remove/approve/post` 驱动；后端接口权限和数据范围未被前端逻辑替代。
- 秒杀首页统计已改为批量接口，服务层和 Mapper 按授权部门范围聚合，单活动统计同时补充数据范围保护。
- 小程序错误上报已统一接入请求失败、全局错误和未处理 Promise；敏感字段脱敏后本地最多保留 20 条，失败保留待后续刷新上报。
- Vite 运行时 patch 已记录适用版本、修复原因、升级验证方式和移除条件；后端会员模块编译通过，小程序 V1.2 定向回归 26 项通过，微信构建通过。
- V1.2 开发门禁已完成；全量测试仍包含本轮无关的既有盘点、首页旧断言、会员和秒杀搜索失败，已与本轮定向结果分开记录。

### 2026-08-04 库存工作台 UI

- 库存查询、库存流水、库存调整已统一为核算周期首页同款“库存工作台”视觉结构：浅蓝渐变头部、浮起 KPI、`section-card` 分区、部门提示、状态反馈和安全区。
- 库存查询补齐 `StateView` 四态、库存摘要和商品卡片层级；库存流水改为上下分层筛选和入库/出库卡片；库存调整统一单据摘要、状态和操作层级。
- 保留现有接口、部门过滤、权限能力、幂等、草稿和库存状态机；未修改数据库和后端业务口径。
- 库存工作台 UI 定向测试 14 项通过，小程序微信构建通过；生成目录已恢复，待体验版视觉验收。

## 阶段执行看板

### V1.0 门禁

- [x] 会话恢复与 Token 刷新首版
- [x] 核算周期首页首版
- [x] 首页请求节流
- [x] 统一响应处理构建回归
- [x] 字典缓存构建回归
- [x] 部门切换写操作竞态二次校验
- [x] FILE 服务修复并通过启动/健康检查
- [ ] DEV 部署和人工验收

### V1.1 门禁

- [x] 库存流水分页筛选
- [x] 多商品库存调整
- [x] 库存调整创建至过账全流程幂等
- [x] 草稿自动保存、恢复和模块/部门/用户隔离
- [x] 表单模块组件化（采购明细、通用字段、费用 OCR 已完成）
- [x] 统一加载/空数据/错误状态组件（已接入库存流水、库存调整和通用表单页）
- [x] V1.1 专项回归
- [ ] V1.1 DEV 部署和人工验收

### V1.2 门禁

- [x] 页面权限统一守卫
- [x] 后端能力驱动按钮
- [x] 秒杀统计批量接口
- [x] Vite patch 文档化和升级验证
- [x] 统一错误上报
- [x] V1.2 定向回归、后端编译和小程序微信构建
- [ ] DEV 部署和人工验收
- [ ] 全量回归、合并前审查和用户确认

### 库存工作台 UI 门禁

- [x] 库存查询统一工作台结构
- [x] 库存流水统一筛选和流水卡片
- [x] 库存调整统一单据、抽屉和底部操作区
- [x] 库存页面定向回归和微信构建
- [ ] 体验版人工视觉验收

## 验收记录模板

| 场景 | 账号/部门 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| 冷启动恢复 | WJS | 保持登录或先刷新 Token | 待验收 | 待验收 |
| 后台停留后回前台 | WJS | 不因正常 Token 过期直接回登录页 | 待验收 | 待验收 |
| 店长周期首页 | 店长主管 | 首屏展示周期净利和回本差额 | 待验收 | 待验收 |
| 主管授权范围 | 主管 | 仅显示授权门店 | 待验收 | 待验收 |
| 投资人周期首页 | 投资人 | 显示授权范围净利和分润 | 待验收 | 待验收 |
| 无当前周期 | 无周期部门 | 显示明确空状态 | 待验收 | 待验收 |
