# junsong-open-sdk

JunSongOpenApi - JavaScript client for junsong-open-sdk
开放平台应用管理与API Key服务接口
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 1.0.0
- Package version: 1.0.0
- Generator version: 7.23.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install junsong-open-sdk --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your junsong-open-sdk from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var JunSongOpenApi = require('junsong-open-sdk');

var defaultClient = JunSongOpenApi.ApiClient.instance;
// Configure API key authorization: apikey
var apikey = defaultClient.authentications['apikey'];
apikey.apiKey = "YOUR API KEY"
// Uncomment the following line to set a prefix for the API key, e.g. "Token" (defaults to null)
//apikey.apiKeyPrefix['Authorization'] = "Token"

var api = new JunSongOpenApi.OpenAppControllerApi()
var openApp = new JunSongOpenApi.OpenApp(); // {OpenApp} 
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.add(openApp, callback);

```

## Documentation for API Endpoints

All URIs are relative to *http://localhost:8080/junsong-open*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*JunSongOpenApi.OpenAppControllerApi* | [**add**](docs/OpenAppControllerApi.md#add) | **POST** /app | 
*JunSongOpenApi.OpenAppControllerApi* | [**approve**](docs/OpenAppControllerApi.md#approve) | **PUT** /app/approve/{appId} | 
*JunSongOpenApi.OpenAppControllerApi* | [**callExport**](docs/OpenAppControllerApi.md#callExport) | **POST** /app/export | 
*JunSongOpenApi.OpenAppControllerApi* | [**changeKeyStatus**](docs/OpenAppControllerApi.md#changeKeyStatus) | **PUT** /app/keys/changeStatus | 
*JunSongOpenApi.OpenAppControllerApi* | [**edit**](docs/OpenAppControllerApi.md#edit) | **PUT** /app | 
*JunSongOpenApi.OpenAppControllerApi* | [**getInfo**](docs/OpenAppControllerApi.md#getInfo) | **GET** /app/{id} | 
*JunSongOpenApi.OpenAppControllerApi* | [**list**](docs/OpenAppControllerApi.md#list) | **GET** /app/list | 
*JunSongOpenApi.OpenAppControllerApi* | [**listAllKeys**](docs/OpenAppControllerApi.md#listAllKeys) | **GET** /app/keys/list | 
*JunSongOpenApi.OpenAppControllerApi* | [**listKeys**](docs/OpenAppControllerApi.md#listKeys) | **GET** /app/keys/{appId} | 
*JunSongOpenApi.OpenAppControllerApi* | [**reject**](docs/OpenAppControllerApi.md#reject) | **PUT** /app/reject/{appId} | 
*JunSongOpenApi.OpenAppControllerApi* | [**remove**](docs/OpenAppControllerApi.md#remove) | **DELETE** /app/{ids} | 
*JunSongOpenApi.OpenInternalControllerApi* | [**getByAppKey**](docs/OpenInternalControllerApi.md#getByAppKey) | **GET** /internal/secret/byKey/{appKey} | 


## Documentation for Models

 - [JunSongOpenApi.AjaxResult](docs/AjaxResult.md)
 - [JunSongOpenApi.OpenApp](docs/OpenApp.md)
 - [JunSongOpenApi.OpenAppSecret](docs/OpenAppSecret.md)
 - [JunSongOpenApi.TableDataInfo](docs/TableDataInfo.md)


## Documentation for Authorization


Authentication schemes defined for the API:
### apikey


- **Type**: API key
- **API key parameter name**: Authorization
- **Location**: HTTP header

